{
  "name": "front-end",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npx prettier --write . && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "prettier": "2.8.7",
    "vite": "^4.2.0"
  }
}
